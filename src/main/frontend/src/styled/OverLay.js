import {styled} from "styled-components";

export const OverLay = styled.div`
  position: relative;
  z-index: 2;
  .place{
    position: absolute;
    left: 10px;
    bottom: 4px;
    font-size: 10px;
    color: gray;
  }
  `